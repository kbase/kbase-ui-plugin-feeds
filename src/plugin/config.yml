## Plugin Configuration
---
package:
  name: feeds
  description: The feeds panel and associated widgets
install:
  routes:
    - path: feeds
      view: feeds
      authorization: true
  menu:
    - name: feeds
      definition:
        path: ["feeds"]
        label: Feeds
        icon: bullhorn
